{
  "name": "rjames",
  "version": "1.0.0",
  "description": "Redesign for 2023",
  "main": "sw.js",
  "scripts": {
    "start": "http-server -p 3000",
    "test": "vitest",
    "e2e": "playwright test",
    "deploy": "cd work && wrangler deploy --env production",
    "deploy:staging": "cd work && wrangler deploy --env staging",
    "deploy:dev": "cd work && wrangler dev",
    "deploy:tail": "cd work && wrangler tail --env production",
    "deploy:rollback": "cd work && wrangler deployments list"
    "e2e": "playwright test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.14",
    "browser-sync": "^3.0.4",
    "postcss": "^8.4.24",
    "postcss-cli": "^10.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "cssnano": "^6.0.1",
    "http-server": "^14.1.1",
    "jsdom": "^27.0.0",
    "playwright": "^1.45.3",
    "vitest": "^3.2.4"
  }
}
