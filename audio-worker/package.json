{
  "name": "rjames-audio-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker streaming audio and media assets from R2 with Range request support and caching",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  },
  "devDependencies": {
    "wrangler": "^3.109.2"
  }
}
